# Shared by the CLI (quest/) and the API backend (backend/)
requests>=2.31
python-dotenv>=1.0
rich>=13.7

# Backend (FastAPI on Koyeb/Zeabur)
fastapi>=0.115
uvicorn>=0.30
websockets>=12  # voice-chat signaling (WebSocket support for uvicorn)
psycopg[binary]>=3.2   # used only when DATABASE_URL is set
psycopg-pool>=3.2      # connection pooling — a fresh TLS handshake per query was the "slow answers" bug
